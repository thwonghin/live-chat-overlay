diff --git a/node_modules/webpack/types.d.ts b/node_modules/webpack/types.d.ts
index 3949ac5..0b17301 100644
--- a/node_modules/webpack/types.d.ts
+++ b/node_modules/webpack/types.d.ts
@@ -6635,9 +6635,7 @@ declare interface PerformanceOptions {
 	 */
 	maxEntrypointSize?: number;
 }
-declare interface Plugin {
-	apply: () => void;
-}
+
 declare interface PnpApiImpl {
 	resolveToUnqualified: (arg0: string, arg1: string, arg2?: any) => string;
 }
@@ -9861,6 +9859,11 @@ declare interface WebpackPluginInstance {
 	 */
 	apply: (compiler: Compiler) => void;
 }
+
+declare abstract class Plugin {
+	apply(compiler: Compiler): void;
+}
+
 declare interface WithId {
 	id: string | number;
 }
@@ -10402,8 +10405,15 @@ declare namespace exports {
 		RuleSetUseItem,
 		Configuration,
 		WebpackOptionsNormalized,
-		WebpackPluginInstance
+		WebpackPluginInstance,
+		// MOD START: Backward compatibility
+		Plugin,
+		PathData as ChunkData,
 	};
+	export namespace compilation {
+		export { Compilation };
+	}
+	// MOD END
 }
 
 export = exports;
